# NextAuth Configuration
NEXTAUTH_SECRET=primebalance-dev-secret-key-change-in-production-2024
NEXTAUTH_URL=http://localhost:3000

# OAuth Providers (optional - credentials login works without these)
GOOGLE_CLIENT_ID=demo
GOOGLE_CLIENT_SECRET=demo
GITHUB_ID=demo
GITHUB_SECRET=demo

# Database Configuration
DB_ROOT_PASSWORD=YourSecureRoot123!
DB_USER_PASSWORD=YourSecureApp456!
DB_NAME=primebalance
DB_USER=primebalance_user
DB_INSTANCE=primebalance-db

# Local development DATABASE_URL (uncomment for local dev with cloud-sql-proxy)
# DATABASE_URL=postgresql://primebalance_user:YourSecureApp456!@127.0.0.1:5432/primebalance